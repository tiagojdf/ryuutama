import {
  StyleSheet,
} from 'react-native'

// import styleVariables from '../../../Config/styleVariables'

export default StyleSheet.create({
  container: {
    flex: 1
  },
  invalid: {
    borderColor: 'red',
    borderWidth: 2,
  }
})
